{# Iterate over the list of posts and render each individually #}
{% for post in posts %}
    {{
        render(
            controller(
                "ez_content:viewLocation",
                {
                    'locationId': post.contentInfo.mainLocationId,
                    'viewType': 'summary'
                }
            )
        )
    }}
{% endfor %}

{{ pagerfanta(posts, 'twitter_bootstrap_translated', {'routeName': location}) }}
